import "../styles/Footer.css";

export default function Footer() {
  return (<footer className="footer">
    <h1>¡It is possible that the first action/http req will delay so much because of the free plan of the hosting that i am using which turns off the service after 30 minutes of inactity and reboot it when a new req is recived!</h1>
  </footer>);
}
