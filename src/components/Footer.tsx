import "../styles/Footer.css";

export default function Footer() {
  return (<footer className="footer">
    <h1>¡Es posible que la primera acción que se haga en la página tarde mucho tiempo en cargar debido a que el host del servidor tumba la página cada 30 min y en cuanto vuelve a recibir una petición tiene que reiniciar todo el servicio!</h1>
  </footer>);
}
